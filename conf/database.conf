{
    include "general.conf"

    database = ${APP_HOME}"/data/binance_database.db",
    table = "crypto_history"
    frequency = "1h"
    market = "BTCUSDT"
    path_save = "data/tmp"
    path_report = "logs/report_errors_rows_binance_vision.txt"
    path_historical_klines = "data/scrapping/binance_vision/klines/data/spot/monthly/klines/"${market}"/"${frequency}
}